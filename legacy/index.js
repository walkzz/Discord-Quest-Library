console.log('Quest process running. Keep this window open while the quest progresses.');
console.log('Press Ctrl+C to exit.');

// Keeps the event loop alive indefinitely without burning CPU cycles
setInterval(() => {}, 1000 * 60 * 60);